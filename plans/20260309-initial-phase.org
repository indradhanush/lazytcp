#+TITLE: lazytcp Initial Phase Plan

* Objective
Deliver a usable first slice of =lazytcp= that makes live packet exploration easier than raw =tcpdump=/=tshark= usage by supporting fast, in-session filter changes.

* Scope
In scope for this phase:
- Single local capture session.
- Terminal UI built with =ratatui=.
- Live packet list with lightweight summaries.
- Interactive filter editing and immediate re-application.
- Packet detail view for selected row.

Out of scope for this phase:
- Full pcap analysis workflows.
- Multi-node/remote capture orchestration.
- Wireshark-level protocol decode breadth.
- Persistent profile/config system.

* Architecture Slice
- =src/domain.rs=
  - Define packet summary/detail models.
  - Define typed filter model and normalization rules.
- =src/capture.rs=
  - Define capture backend trait.
  - Implement initial backend using external tool invocation with line-buffered output.
  - Convert backend output into domain packet events.
- =src/api.rs=
  - Expose non-interactive API contracts so tests and agents can verify behavior without driving the TUI.
- =src/lib.rs=
  - Export stable library modules for test and automation entrypoints.
- =src/app.rs=
  - Maintain app state: capture state, packet ring buffer, selected row, active filter, filter input mode.
  - Handle key actions and state transitions.
- =src/ui.rs=
  - Render: header/status, packet table, filter input bar, packet detail pane.
- =src/main.rs=
  - Terminal lifecycle, event loop, and graceful shutdown.

* Milestones
** M1: App Skeleton and Event Loop
Deliverables:
- Basic =ratatui= app shell with startup/shutdown correctness.
- Keyboard handling baseline: =q=, =j/down=, =k/up=, =tab= for pane/focus switching.
- Placeholder panes for packet list, filter input, details.

Acceptance criteria:
- App launches and exits cleanly without terminal corruption.
- Focus/selection movement is visible and stable.

** M2: Capture Backend Integration
Deliverables:
- Capture backend trait with one concrete implementation.
- Background capture task that streams packet summaries to app state.
- Backpressure control via bounded channel/ring buffer.

Acceptance criteria:
- Live packet rows appear continuously from local interface/input source.
- App remains responsive while capture is active.
- Capture process is stopped reliably on exit.

** M3: Interactive Filtering
Deliverables:
- Filter input mode (enter, edit, submit, clear).
- Typed filter representation supporting:
  - host/source/destination
  - protocol/traffic type
  - port
- Re-apply filter live without restarting app.

Acceptance criteria:
- User can refine filter iteratively in one session.
- Packet list updates reflect the active filter with low latency.
- Invalid filter input yields non-crashing inline feedback.

** M4: Packet Detail Drilldown
Deliverables:
- Select packet row and render decoded detail pane.
- Display key fields (timestamp, src, dst, protocol, ports, length, summary text).

Acceptance criteria:
- Detail pane tracks selected packet correctly.
- Navigation does not interrupt capture stream.

** M5: Stabilization and Tests
Deliverables:
- Unit tests for filter parsing/normalization/matching in =domain.rs=.
- Unit tests for capture argument construction and parser boundaries in =capture.rs=.
- Contract tests that:
  - Generate/read a =.pcap= fixture file.
  - Run =tcpdump= over that fixture.
  - Compare typed library API outputs with direct =tcpdump= outputs for parity.
- Error-path handling for backend startup failure and malformed output.

Acceptance criteria:
- Critical domain and capture logic paths covered by deterministic tests.
- Core verification is executable through the library API; no interactive TUI driving is required for automated checks.
- User-facing error states are explicit and recoverable.

* Progress Snapshot (2026-03-10)
- M1 status: done.
  - Implemented terminal lifecycle, event loop wiring, and baseline key navigation (=q=, =j/down=, =k/up=, =tab=).
- M2 status: in progress.
  - Capture trait scaffolding exists and runtime currently loads from a required =.pcap= file through the API.
  - Live streaming capture integration from a running backend is not implemented yet.
- M3 status: not started.
  - Interactive in-session filter editing/re-apply in the TUI is still pending.
- M4 status: in progress.
  - Packet list selection and detail pane rendering are implemented for loaded packet data.
  - Behavior while a live capture stream is active is pending with M2.
- M5 status: in progress.
  - API contract tests against =tcpdump= with generated =.pcap= fixtures are implemented and passing.
  - Additional unit coverage for full filter model/normalization and capture error-path hardening remains.

Current implemented baseline:
- Project renamed to =lazytcp=.
- Runtime requires =lazytcp <pcap-file> [tcpdump-filter...]= and no longer depends on mock runtime rows.
- CLI error UX for missing/invalid args is user-friendly (usage/help output, explicit exit codes).
- =make= runs =cargo fmt=, =cargo test=, and =cargo check=.

* Execution Order
1. M1 app shell and input loop.
2. M2 streaming capture path.
3. M3 live filter model + UI flow.
4. M4 detail pane integration.
5. M5 test hardening and cleanup.

* Risks and Mitigations
- Risk: External capture output variability.
  - Mitigation: Isolate parser; keep strict fallback handling and unit fixtures.
- Risk: UI lag under high packet volume.
  - Mitigation: Bounded buffers, truncation strategy, incremental rendering.
- Risk: Filter UX complexity grows too quickly.
  - Mitigation: Start with compact typed grammar and incremental extensions.

* Definition of Done (Initial Phase)
- Operator can run one capture session, browse packets, edit filters live, and inspect packet details.
- Terminal state is always restored on normal exit and error exit.
- Core logic is covered by deterministic tests.

* Verification Outcomes
Record outcomes here as milestones are completed.

- 2026-03-09: M1 implementation started.
- 2026-03-09: =cargo fmt= passed.
- 2026-03-09: =cargo test= failed in this environment due to blocked network access to =index.crates.io= (dependency fetch failure).
- 2026-03-09: =cargo check= failed in this environment due to blocked network access to =index.crates.io= (dependency fetch failure).
- 2026-03-09: Added non-interactive library API contracts and pcap+tcpdump parity contract tests.
- 2026-03-09: =cargo fmt= passed (after API+contract test changes).
- 2026-03-09: =cargo test= passed (unit tests + contract tests).
- 2026-03-09: =cargo check= passed.
- 2026-03-09: Runtime no longer uses mock packet rows; CLI now requires a =.pcap= path and loads through the library API.
- 2026-03-09: =cargo fmt= passed (after pcap CLI wiring).
- 2026-03-09: =cargo test= passed (unit tests + contract tests).
- 2026-03-09: =cargo check= passed.
- 2026-03-09: Improved CLI error UX for missing/invalid args; usage/help now prints clean user-facing text (no Rust debug struct output).
- 2026-03-09: =cargo fmt= passed (after CLI UX update).
- 2026-03-09: =cargo test= passed (unit tests + contract tests).
- 2026-03-09: =cargo check= passed.
- 2026-03-09: Project references renamed from =tcpdump-tui= to =lazytcp= (crate/bin/docs/plan strings).
- 2026-03-09: =cargo fmt= passed (after rename updates).
- 2026-03-09: =cargo test= passed.
- 2026-03-09: =cargo check= passed.
- 2026-03-10: Removed milestone/placeholder wording from runtime UI strings.
- 2026-03-10: Added =Makefile= default target to run =cargo fmt=, =cargo test=, and =cargo check=.
- 2026-03-10: =make= passed.
